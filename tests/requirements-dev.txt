# Test-only dependencies. Install with:  pip install -r tests/requirements-dev.txt
# The suite runs fully offline — heavy SDKs (supabase, langchain, openai, mem0,
# redis) are stubbed in tests/conftest.py, so only these are needed to run it.
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
pydantic>=2.9.0
fastapi>=0.115.0
httpx>=0.27.0
